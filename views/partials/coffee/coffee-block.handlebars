{{#each Coffees}}
	{{#unless this.drank}}
		<li>
			{{id}}. {{name}}

			<button class="drink" data-id="{{this.id}}" data-newdrank="{{this.drank}}">
				Drink Coffee!
			</button>

	{{!-- 		<button class= "delete-coffee" data-id="{{this.id}}"><i class="far fa-trash-alt"></i>
			</button> --}}
		</li>
	{{/unless}}
{{/each}}